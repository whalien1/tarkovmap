# TarkovMap 后续迭代借鉴记录（待立项）

> 用途：记录从参考项目（缇尔蒂 tilty 线上站 /kaedeori.com、开源前端 `ref/tarkov-tilty-frontend`、`ref/Tarkov_webmap`）中提取、**尚未在 v1.0 落地**、但有明确借鉴价值的思路与素材。
> 状态说明：v1.0 已完成。本文档是「备忘 + 预研」，不承诺排期。每项在独立评估后决定是否进版本计划。
> 对应主文档：《TarkovMap_MVP_开发文档.md》（下称“主文档”）、《开发计划.md》。

---

## 0. 背景一句话

kaedeori.com（缇尔蒂）就是主文档 §5 引用的开源前端 `ref/tarkov-tilty-frontend` 的线上完整版部署。TarkovMap v1.0 的数据模型、坐标体系、截图定位、四元数→朝向，其源头即这套东西（经 `ref/Tarkov_webmap` 的 `ScreenshotCoordinateParser` 与 `Tools/MapPackBuilder` 落地）。
因此本文档三个条目都聚焦「**尚未被 v1.0 吸收，且不越主文档 §4 红线**」的增量（项 3 为数据源评估记录，特别标注其不越红线与否的取舍）。

>> 红线摘要（主文档 §4.3 / §63）：离线、单一职责、无账号/社区/API/云同步、不碰游戏进程、不读内存、不解析游戏日志。凡需联网数据的借鉴（跳蚤、任务清单、藏身处、队友同步等）一律排除，不做进本文档。

---

## 项 1 —— 楼层 / 高度区间机制（借鉴缇尔蒂）

### 借鉴思路
缇尔蒂不是做成「楼层切换 UI」，而是在**数据模型层**用高度区间表达楼层：

- 每层带 `extents: [{ height: [min, max] }]`。
- 切层时，按 `position.y` 是否落在某层的高度区间内来**过滤该层应显示的点位**；非活动层点位 `opacity 0.1` 淡化。

价值：多层图（工厂、中心区 21+、实验室等）不必为每层拆份坐标，只要点位带 `y` 高度，一个模型就能支撑楼层显隐。

### 与 TarkovMap v1.0 现状的差距
- 主文档 §7 明确 v0.1 不做室内/多楼层；`BuildMap` 当前用 `heightRange` 仅做「主层高度过滤」（`Program.cs:96-102`），把楼上层位直接丢弃。
- 主文档 §16 已预留「`Y` 仅随截图解析保留，不用于楼层」。
- 也就是说：**v1.0 数据模型不存楼层，点位是主层过滤后的扁平结果。** 若未来要楼层，重新生成、排序、切层逻辑都得补。

### 实现方式（建议，供立项时简化）
不推翻 v1.0，分两步走：

1. **数据层预留（可立即做，零成本）**：在 `map.json` schema 中增加可选字段，例如
   ```json
   "layers": [
     { "floor": 0, "name": "地面层", "heightRange": [0, 20] }
   ]
   ```
   同时在每个 Marker 上保留原始 `position.y`（当前 `MarkerOut` 只存 `x/z`，丢了 `y`）。让未来楼层功能可用现有数据恢复，不用重采。
2. **功能层启用（v0.3 候选，需另排期）**：`MapCanvas` 增加楼层显隐；按 Marker 的 `y` 与活动层 `heightRange` 过滤；切换时 `Invalidate()`（符合主文档 §20 事件驱动，不引 RTL 循环）。

### 所需素材
- 数据：各多层图的点位 `position.y`（须在 MapPackBuilder 导入时保留，不再丢弃）。当前 `ref` 数据源 `maps_detail.json` 的 spawns/lootContainers 已带 `position.y`，可直接保留。
- UI：楼层下拉/切换（经典 Windows 工具样式，参照主文档 §31–36）。
- 图标：无需新增，复用现有点位图标。

### 取舍 / 风险
- v1.0 文件已用「主层过滤」生成，若启用楼层需**重新生成**全部受影响地图（可一键 `MapPackBuilder.exe` 重跑）。
- 切层时楼上层位与楼下点位可能重叠，需按 `y` 的优先级或 `opacity` 弱化，避免视觉混乱。

---

## 项 2 —— 游戏内时钟（借鉴缇尔蒂）

### 借鉴思路
缇尔蒂在交互地图上显示“塔科夫游戏内时间”：真实时间按 7 倍流速推进，并带固定时区偏移。参考公式（缇尔蒂 `MapInfo`）：

```
塔科夫时钟 = ( realTime * 7 % 24h ) 的换算 + 时区偏移
```

（缇尔蒂源码中的形式整体为 `realTime * 7 % 24h - 5h` 一类的偏移表达式，具体偏移量以实际对表校准为准。）

### 与 TarkovMap v1.0 现状的差距
- 未实现；v1.0 状态栏只显示缩放比 + 世界坐标。
- 该功能**纯本地离线计算**（基于本机时间），不触主文档 §4.3 红线，是安全加分项。

### 实现方式（建议）
- 在 `StatusStrip` 右侧增一格游戏内时钟，随本机时间每秒/每帧刷新（用 `Timer` 低频刷新即可，频率对齐秒级，不带来持续绘制负担——注意主文档 §20 禁止 60FPS / Render Loop，时钟是文本刷新，用 1s `Timer` 而非重绘画布）。
- 存储偏移与流速为常量（或进 `config.json`），便于校准。
- 说明文字标注「游戏内时间」，避免与本地时间混淆。

### 所需素材
- 无外部素材。仅需：
  - 真实时间的 7 倍流速换算公式；
  - 时区偏移常量（由一个已知对表时刻校准出）；
  - 是否展示在状态栏与 UI 文案（中文）。

### 取舍 / 风险
- 偏移/流速若按服务端（缇尔蒂完整版）规则而非本地游戏规则，可能不完全一致。建议以「逻辑时钟公式 → 用户本地实测对照界面截图」校准，与主文档阶段 5 的工厂校准思路一致。
- 纯自用可接受近似；如需精确，需记录校准来源与日期。

---

## 附：一项已读但建议排除的借鉴（勿做）

- **跳蚤物价 / 任务物品清单 / 藏身处 / 队友同步 / 画中画 / 屏幕滤镜**（缇尔蒂完整版与 `ref/Tarkov_webmap` WinUI 版具备）：
  这些大都依赖联网数据源 + 账号/API，与主文档 §4.3、§63 “只做离线地图” 的产品定义直接冲突。除非未来明确扩展产品定位，否则不进入本文档的立项候选。
  若有一天考虑，需先重写“一句话产品定义”，并评估离线缓存的可行性与合法授权（参照主文档 §5 数据来源复用原则）。

---

## 项 3 —— 第二套点位数据源（缇尔蒂）交叉补全【已评估：维持现状】

> 2026-08-25 补充：本节记录的是 v1.0 旧全类别数据链评估。当前 MapData 核心类别已经改由 `json.tarkov.dev/pve` 生成，但仍未引入缇尔蒂作为第二套合并源，因此“不合并缇尔蒂”的结论继续有效。

### 背景（2026-08 核实结论）
TarkovMap v1.0 只接入 `ref/Tarkov_webmap/data/maps_detail.json` 一套源（`Tools/MapPackBuilder/Program.cs` 仅读该文件）。缇尔蒂另有一份独立的 `src/data/interactive_maps.ts`，**尚未有任何导入器读取**。

同图点位数对比（TarkovMap vs 缇尔蒂，总量 约8194 vs 约9990）：

| 地图 | 物资容器 TarkovMap | 物资容器 缇尔蒂 | 出生点 TarkovMap | 出生点 缇尔蒂 |
|---|---|---|---|---|
| streets-of-tarkov | 1225 | 1278 | 218 | 397 |
| ground-zero | 398 | 约1046 | 114 | 约324 |
| interchange | 297 | 823 | 157 | 约184 |
| reserve | 736 | 991 | 118 | 约156 |
| shoreline | 536 | 756 | 210 | 约239 |
| lighthouse | 533 | 533 | 132 | 约198 |
| customs | 551 | 415 | 237 | 约221 |

立交桥物资容器差约 2.8 倍、街区出生点差近 1 倍，TarkovMap 偏薄。

### 关键判断
差别**不一定是“缺漏”，而是两套社区数据各自的覆盖口径不同**（来源、坐标、拆分粒度都不同）。并非 MapPackBuilder 有打点能力没启用。

### 决策（已定，2026-08）
**采用路线 B：保持现状，不引入第二套导入器。** 理由：
- v1.0 数据来源单一、可追溯（source 元数据统一标注 Tarkov_webmap/`the-hideout`）。
- 引入缇尔蒂需新增导入器 + 坐标体系换算（bounds/transform/coordinateRotation 对齐）+ 同坐标去重，工作量大、验证成本高，收益仅为覆盖度更接近线上站，而主文档本就定位“只做地图、不过度堆料”。
- 若未来确需更厚覆盖度，再在本项路线 C（只补物资容器+出生点）或路线 A（全量合并）中评估排期。

### 若未来重启（备查）
- 路线 A：新增缇尔蒂导入器，全量合并 + 按来源去重，覆盖度最接近线上站。
- 路线 C：仅对缇尔蒂明显多、v1.0 偏薄的类型（物资容器、出生点）做定点合并，控制工作量。
- 无论哪条：需在 schema 标记每点第二来源，坐标换算以 `worldBounds`/`coordinateRotation` 对齐为准（参照主文档 §16–17）。

---

## 项 4 —— tarkov.dev API 作为更新源【已落地，旧结论已推翻】

### 背景（2026 检查结论）
tarkov.dev 官方 GraphQL API（`https://api.tarkov.dev/graphql`，the-hideout 社区，GPL-3.0）免费、开源、实时更新，提供完整地图数据节点：`maps`（含 extracts/spawns/loot/bosses/hazards/key requirements）、`loot-containers`、`stationary-weapons` 等，与 `Tools/MapPackBuilder/Program.cs` 所需类别一一对应。理论上可作为新地图（如破冰船）的数据更新源。

> 说明：实测 GraphQL 需 POST，只读工具仅能 GET 故返回 400/422，属请求方式限制非 API 故障；未实际验证破冰船是否已被 API 收录。

### 当前决策（2026-08-25）

已按“只在构建期联网”的路线接入 `json.tarkov.dev/pve`，不会改变客户端离线和不触碰游戏进程的红线。MapPackBuilder 会保存原始响应、时间和 SHA-256，可从固定快照离线重放；Bounds 与方向仍来自项目独立校准配置。旧 `ref/` 入口只保留基线回归用途。

新地图仍不会自动启用：只有完成底图、Bounds、方向和截图定位验证后才能进入正式数据包。

---

## 待办（由用户决定是否排期）
- [ ] 数据层：Marker 保留 `position.y` + schema 增加 `layers`（可零成本先做）。
- [ ] 项 1 楼层功能立项（v0.3 候选）。
- [ ] 项 2 游戏内时钟立项（v0.2/v0.3 候选）。
- [ ] 项 3 第二套数据源：**已定维持现状（路线 B）**；仅当覆盖度成为问题再评估路线 C/A。

---

## 项 5 —— 小地图迭代方向（v1.1 已落地基础版，以下为增强候选）

> v1.1 已交付：无边框置顶小地图、固定 Marker 组（撤离点/Boss/危险区/地区名）、拖动/缩放、形状/大小/透明度设置、生命周期管理。以下为用户 Q&A 中明确"后续再迭代"的选项：

- [ ] 多显示器适配（v1.1 默认单显示器）。
- [ ] 小地图 Marker 组可配置（当前固定四类，未来可让用户自选）。
- [ ] 小地图形状扩展（当前方/圆，用户首选方形已满足）。
- [ ] 河畔之路（信号弹）坐标校准（立交桥，估算值待实测）。
- [ ] 新地图（破冰船等）导入：通用 PNG 导入器 + 两点定位法校准（MapPackBuilder 已留入口）。
