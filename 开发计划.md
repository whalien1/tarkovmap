# TarkovMap (塔科夫地图) 分阶段开发计划

> **状态：v1.0 已发布（2026-08-24）**。全部 6 个阶段 + UI 打磨均通过验收。

## v1.0 完成记录

- 阶段 1–6 全部通过用户验收
- UI 打磨轮：撤离点大图标+名称、标注加粗描边、危险区红色多边形（即死机制默认开启）、Boss 红圈红名+同点合并、撤离点数据补全（去除高度过滤）、河畔之路（信号弹）手工补录、寻血猎犬（当前版本不存在）排除
- 性能：图片最大边长 3000px、画布只画可见区域；实验室 60MB 级别
- 维护通道：`Tools/MapPackBuilder`（官方数据一键重新生成）+ `Tools/manual_overrides.json`（手工补录不丢失）+ `ExcludedBosses` 排除名单
- 待办：河畔之路坐标为估算，待用户本地模式实测截图校准

> 面向 0 编程基础。每一个阶段做完都要经过用户亲自验收，确认通过后才能进入下一阶段。
> 每阶段交付一个双击可运行的 exe + 傻瓜验收清单，用户不需要碰命令行。

## 已确认决策（2026-08-24 与用户一问一答确认）

- **截图文件名格式**（游戏自带截图功能生成，已扫描真实目录 110 张验证）：
  `YYYY-MM-DD[HH-mm]_X, Y, Z_q0, q1, q2, q3_FOV (序号).png`
  - 扩展名为 `.png`；FOV 字段忽略；`(0)` 是同秒多张的序号
  - 约 5% 文件无坐标（结算/仓库画面截图），解析器直接跳过（TryParse=false），不提示不报错
- **截图目录**：`C:\Users\whalien\Documents\Escape from Tarkov\Screenshots`（游戏默认目录）
- **四元数→朝向**：参考实现 `ref/Tarkov_webmap/src/TarkovMapLocator.Core/Maps/ScreenshotCoordinateParser.cs`（C# 正则 + Yaw 公式，与真实文件名格式完全匹配）；最终用工厂 4 张定向校准截图锁定公式
- **数据源优先级**：`ref/Tarkov_webmap`（中文点位名、bounds/transform/coordinateRotation 现成、SVG、分类图标）为主；`ref/tarkov-tilty-frontend` 为交叉验证备用
- **开发主图**：海关（阶段 2/3）；**定位校准图**：工厂本地模式（阶段 5，用户将提供东南西北 4 张带朝向说明的校准截图）
- **街区验证点**：X=-239.36, Z=109.80 = 内务部附近（用于阶段 4 坐标抽查）
- **地图范围**：10 张全做；**工厂等多层图只做地面主层**（地下/楼上截图时箭头仍画在主层对应 X/Z，v0.3 再做楼层）
- **新地图（如破冰船）**：MapPackBuilder 设计两种导入器：①参考项目数据导入器；②通用 PNG 导入器（自制高清图 PNG + 手工边界校准，两点定位法算线性映射）。自制图 = 底图 + 定位箭头，无分类 Marker
- **图标**：用参考项目现成图标（extract_pmc.png 等）；**纯自用不公开发布**，授权不阻塞；数据照常记录来源（ref 项目无 LICENSE，若未来公开需替换资源来源）
- **界面语言**：中文
- **环境**：用户机器已装 .NET SDK 10.0.400；GitHub 可直连；两个参考仓库已下载到 `ref/`
- **安全红线**：不碰游戏进程、不读内存、不注入、不拦截/修改游戏文件、无 Overlay——只读截图文件名
- **UI 设计**：严格参照开发文档 §31–36（经典 Windows 工具式布局）

---

## 技术栈

C# + .NET 10 + WinForms，Framework-dependent 发布。客户端零第三方 NuGet；MapPackBuilder（开发工具）允许用 SVG 渲染依赖。

---

## 阶段 1 —— 项目骨架 + 一个能跑的空窗口

**实现**：`dotnet new winforms` 生成项目；主窗口标题 "TarkovMap"，1280×800；关闭即完全退出；建立 `Controls/ Models/ Services/ Infrastructure/` 目录。

**验收**：
- ✅ 双击 exe 弹出空窗口，标题 TarkovMap
- ✅ 点 × 窗口关闭，进程结束（不留后台）
- ✅ 项目目录结构齐全

## 阶段 2 —— MapCanvas：地图底图 + 拖动 + 缩放

**实现**：自定义画布控件；载入海关 PNG；Fit 适配窗口；左键拖动平移；滚轮以鼠标为中心缩放（1.15 倍步进）；状态栏显示缩放比 + 鼠标世界坐标；双缓冲；事件驱动绘制（空闲不重绘）。

**验收**：拖动跟手无闪烁；缩放中心 = 鼠标位置；空闲 CPU≈0%；Resize 正常。

## 阶段 3 —— Marker 显示：点位 + 图标 + 分类开关 + 点击

**实现**：Marker 模型 + 图标缓存；世界→图片坐标映射；分类勾选开关（撤离点/出生点/Boss 默认开，门锁等默认关）；点击显示"名称 + 类别"；视口裁剪。

**验收**：点位与真实地图位置一致；开关即时生效；点击显示名称类别；全开不卡。

## 阶段 4 —— MapPackBuilder + 全部 10 张地图数据

**实现**：独立 console 工具；解析 ref 数据 → 生成 TarkovMap 自有 schema 的 `Data/`（maps.json + map.json + map.png + 图标）；SVG→高分辨率 PNG；通用 PNG 导入器入口（为未来新地图/自制图）；每张图输出核查报告。

**验收**：一键生成全部 10 张；抽查 ≥3 张已知点位准确（含街区"内务部"验证点）；客户端开任意图正常。

## 阶段 5 —— 截图定位：监听 + 玩家箭头 + 自动居中

**实现**：截图目录选择 + 持久化；FileSystemWatcher（防抖）；文件名解析（参考 ScreenshotCoordinateParser）；四元数→朝向（用工厂 4 张定向校准截图锁定）；玩家箭头（固定屏幕像素大小）；定位自动居中、Zoom 不变；坐标不匹配只提示。

**验收**：放入测试文件→箭头出现并居中、Zoom 不变；朝向校准正确；不匹配时提示不报错；重启后目录还记得。

## 阶段 6 —— 经典 Windows UI + 配置 + 稳定性

**实现**：MenuStrip / 左侧 GroupBox / StatusStrip / SplitContainer（按文档 §32 线框图）；置顶开关；ConfigService 容错；ErrorLogger；单实例 Mutex；PerMonitorV2 DPI。

**验收**：逐张切图内存不涨；config 损坏回退默认；置顶记住；单实例；4K 不糊；空闲 CPU≈0、无网络；关闭无后台进程。

---

## Backlog（本版不做）

- Marker 搜索、快捷过滤（v0.2 候选）
- 室内地图 / 多楼层 / Y 高度判断（v0.3 候选）
- Tile 金字塔（仅当 PNG 实测成为瓶颈，v0.4 候选）
- 自制地图的可分类 Marker 数据（需手工补点位）
