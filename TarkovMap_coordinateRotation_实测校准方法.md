# TarkovMap `coordinateRotation` 实测校准方法

> 文档用途：用于逐张地图校准 `coordinateRotation`，不修改通用朝向算法。  
> 适用版本：TarkovMap v1.1.1 及后续兼容版本  
> 当前锁定显示公式：
>
> ```text
> 玩家箭头角度 = Yaw + coordinateRotation + 90°
> ```
>
> 本文只解决“每张地图的 `coordinateRotation` 应该是多少”这一数据校准问题。

---

## 1. 校准原则

当前通用朝向算法已经通过工厂、中心区、海关实测，不应因单张地图方向异常而修改。

如果某张地图的玩家箭头方向错误，优先检查 `coordinateRotation`，并通过 `Tools/MapPackBuilder` 中的 `RotationOverrides` 做地图级修正。

原则：

- 不修改 `PlayerDirectionService`
- 不修改统一的 `+90°` 常量
- 不为单张地图写特殊 UI 逻辑
- 地图朝向问题归类为“地图数据校准”
- 校准结果应记录样本与日期
- 最终值优先考虑 `0 / 90 / 180 / 270`，但算法不强制限制为四个值

---

## 2. 推荐方法：直线移动双截图反推

推荐正式校准方法：

> 在两张截图之间，玩家基本保持同一朝向，并沿当前面朝方向直线移动一段距离。

不要求玩家知道游戏世界中的“正北”，也不要求找一个绝对朝向明确的地标。

每张截图文件名已包含：

```text
X / Y / Z
Quaternion
```

TarkovMap 可以得到：

```text
Quaternion → Yaw
World X/Z → MapCoordinateService.WorldToImage() → 图片坐标
```

因此两张截图可以同时得到：

```text
玩家实际移动方向
+
玩家截图记录的朝向 Yaw
```

二者之间的角度差即可反推 `coordinateRotation`。

---

## 3. 单组测试操作方法

以“森林”为例。

### 3.1 选择测试位置

寻找：

- 比较开阔
- 能连续直走
- 不容易被障碍物迫使转向
- 至少能走约 20～40 米

的区域。

不要求道路严格南北向。

### 3.2 截图 A

站稳后：

1. 面向准备前进的方向
2. 不再移动鼠标
3. 游戏内按截图键
4. 得到截图 A

记作：

```text
A:
X1
Z1
Yaw1
```

### 3.3 直线移动

保持鼠标基本不动，只使用 `W` 向前直走。

推荐距离：

```text
20～40 米
```

最低建议：

```text
15 米
```

不推荐小于 10 米。

### 3.4 截图 B

停止后：

1. 不改变视角
2. 再次截图
3. 得到截图 B

记作：

```text
B:
X2
Z2
Yaw2
```

---

## 4. 样本质量要求

### 4.1 移动距离

推荐：

```text
20～40m
```

质量判断：

```text
≥20m        优秀
15～20m     可用
10～15m     勉强
<10m        建议重测
```

### 4.2 朝向变化

计算 A/B 两张截图的最小角度差：

```text
YawDelta
```

质量判断：

```text
≤5°         优秀
5°～10°     可用
>10°        建议废弃样本
```

如果移动时明显转动鼠标，则这组测试不应采用。

---

## 5. 计算步骤

### 5.1 世界坐标转图片坐标

分别调用：

```text
WorldToImage(A.X, A.Z)
WorldToImage(B.X, B.Z)
```

得到：

```text
A = (Ax, Ay)
B = (Bx, By)
```

### 5.2 地图上的移动方向

计算：

```text
dx = Bx - Ax
dy = By - Ay
```

由于图片坐标 Y 轴向下，而地图视觉上的“向上”为 0°：

```text
MapBearing = atan2(dx, -dy)
```

转换为角度并归一化到 `0°～360°`：

```text
地图向上 = 0°
地图向右 = 90°
地图向下 = 180°
地图向左 = 270°
```

### 5.3 玩家平均 Yaw

当 A/B 朝向变化很小时，可使用圆周平均值 `AverageYaw`。

注意：

```text
YawA = 359°
YawB = 1°
```

平均值应接近 `0°`，而不是 `180°`。

### 5.4 反推 Rotation

当前正式显示公式：

```text
ArrowAngle = Yaw + coordinateRotation + 90°
```

因此：

```text
coordinateRotation
=
MapBearing - AverageYaw - 90°
```

最后归一化到 `0°～360°`。

---

## 6. 不要只测一组

每张地图：

```text
最低：2 组
推荐：3 组
```

而且至少选择两个明显不同的移动方向。

例如：

```text
测试 1：
A ─────────→ B

测试 2：
C
│
│
▼
D
```

这样可以避免某一方向上的偶然误差。

---

## 7. 推荐正式验收标准

一张地图建议满足：

```text
样本数 ≥ 2
推荐样本数 = 3
每组移动距离 ≥ 20m
每组 Yaw 变化 ≤ 5°
至少包含两个不同移动方向
```

例如得到：

```text
样本 1：89.4°
样本 2：90.8°
样本 3：90.1°
```

则可判定：

```text
coordinateRotation = 90°
```

---

## 8. 异常结果处理

### 8.1 接近标准角

例如：

```text
88.9°
91.3°
89.7°
```

推荐值：`90°`。

### 8.2 三组差异很大

例如：

```text
90°
92°
271°
```

不要直接取平均。

优先检查第三组：

- 是否移动太短
- 是否移动时转动鼠标
- 是否撞障碍改变路线
- 是否选错截图
- 是否截图属于其他地图

### 8.3 得到奇怪角度

例如稳定得到：

```text
136°
138°
137°
```

不要擅自吸附到 90° 或 180°。

应重新检查：

- 图片方向
- `reverseCoordinate`
- World→Image 映射
- 当前 `map.json` 是否正确
- 参考数据中的 rotation 定义
- 测试过程是否真的沿视角直走

只有确认数据后才能决定是否接受非 90° 倍数。

---

## 9. 反向验证

推荐至少对一张地图做一次反向验证。

流程：

```text
A → B
```

测完后转身约 180°：

```text
B → A
```

理论上：

```text
移动方向差 ≈ 180°
Yaw 差 ≈ 180°
```

但两组反推出的 `coordinateRotation` 应基本相同。

---

## 10. 推荐开发工具：RotationCalibrator

建议在：

```text
Tools/RotationCalibrator/
```

增加一个纯开发期工具。

它不进入正式 TarkovMap 客户端，也不改变客户端功能。

### 输入

```text
地图 map.json
截图 A 文件名
截图 B 文件名
```

### 输出示例

```text
地图：森林

截图 A
X = ...
Z = ...
Yaw = ...

截图 B
X = ...
Z = ...
Yaw = ...

移动距离：27.2 m
Yaw 变化：1.8°
地图移动方向：181.4°

推算 coordinateRotation：89.6°
最近标准角：90°
样本质量：高
```

多组输入后：

```text
样本 1：89.6°
样本 2：90.7°
样本 3：90.1°

平均结果：90.13°
最大离差：0.7°

推荐：
["woods"] = 90.0
```

工具不得自动修改 `RotationOverrides`，最终写入必须人工确认。

---

## 11. RotationCalibrator 防呆规则

### 移动距离

```text
<10m → 拒绝样本
```

### Yaw 变化

```text
≤5°       高质量
5～10°    警告
>10°      拒绝或强烈建议重测
```

### 标准角提示

如果结果接近：

```text
0 / 90 / 180 / 270
```

可以提示最近值，但不要自动写入数据。

---

## 12. 校准记录模板

每张地图完成后建议留下：

```markdown
## woods / 森林

日期：2026-xx-xx

| 样本 | 移动距离 | Yaw变化 | 推算Rotation |
|---|---:|---:|---:|
| 1 | 27.2m | 1.8° | 89.6° |
| 2 | 31.5m | 2.1° | 90.7° |
| 3 | 24.8m | 0.9° | 90.1° |

最终值：90°
结论：通过
```

---

## 13. 写入 RotationOverrides

只有实测通过后才更新：

```csharp
private static readonly Dictionary<string, double> RotationOverrides =
    new(StringComparer.Ordinal)
{
    ["ground-zero"] = 90.0,
    ["customs"] = 90.0,
    ["woods"] = 90.0, // YYYY-MM-DD，3 组直线移动实测
};
```

然后：

```text
重新运行 MapPackBuilder
→ 生成 Data
→ 重新启动 TarkovMap
→ 再做一组最终现场验证
```

---

## 14. 当前校准状态

已通过实测：

```text
工厂
中心区
海关
```

仍需逐图确认：

```text
街区
立交桥
实验室
灯塔
储备站
海岸线
森林
迷宫
以及后续新增地图
```

其中立交桥另有独立待办：

```text
河畔之路（信号弹）
```

其 X/Z 仍需真实截图校准，该问题与 `coordinateRotation` 不属于同一问题。

---

## 15. 最终固定流程

```text
真实截图 A
↓
直线移动 ≥20m
↓
真实截图 B
↓
验证 Yaw 变化 ≤5°
↓
WorldToImage
↓
计算地图移动方向
↓
反推 coordinateRotation
↓
至少再做 1～2 组
↓
人工确认
↓
写入 RotationOverrides
↓
MapPackBuilder 重建
↓
最终游戏内验证
```

任何单张地图方向异常：

> **先校准地图数据，不改通用朝向算法。**
